package assignment12;

public class BorrowService {

    public String borrowBook(String userId, String bookId) {
        // Example logic (just a placeholder)
        return "User " + userId + " borrowed Book " + bookId;
    }
}

